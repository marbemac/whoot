<%= @user.first_name %>, there’s been a lot going on since you last logged in. We have a few notifications for you!

<% @notifications.each do |notification| %>
  <% if notification.type == 'follow' %>
      - You have <%= pluralize(notification.triggered_users_notify_count.to_i, 'new follower') %>! <%= notification.triggered_users_notify_string %> <%= notification.triggered_users_notify_count > 1 ? 'are' : 'is' %> now following you.
  <% elsif notification.type == 'comment' %>
      - You have <%= pluralize(notification.triggered_users_notify_count.to_i, 'new comment') %> on your post! <%= notification.triggered_users_notify_string %> commented.
  <% end %>
<% end %>